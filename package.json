{
  "name": "dil-core",
  "version": "0.1.0",
  "description": "Data Integration Loop — dil-core",
  "type": "module",
  "bin": {
    "dil": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "tsc && node --test \"dist/**/*.test.js\"",
    "dev": "tsc --watch"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "typescript": "^5.5.0"
  }
}
